# ------------------------------------------------------------------------------
# Inference Gateway base
#
# Resources shared by every gateway topology (single-pool and epd-pools).
# Today this is just the Gateway CR; topology-specific siblings
# (single-pool/, epd-pools/) layer their own InferencePools, EPPs, and
# HTTPRoutes on top.
# ------------------------------------------------------------------------------
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- gateways.yaml
