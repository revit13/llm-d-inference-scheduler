apiVersion: v1
kind: Service
metadata:
  name: multimedia-cache
  labels:
    app: multimedia-cache
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: multimedia-cache

