apiVersion: v1
kind: Service
metadata:
  name: mock-downloader1
  labels:
    app: mock-downloader1
spec:
  selector:
    app: mock-downloader1
  ports:
  - name: http
    protocol: TCP
    port: 9000
    targetPort: 9000
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: mock-downloader2
  labels:
    app: mock-downloader2
spec:
  selector:
    app: mock-downloader2
  ports:
  - name: http
    protocol: TCP
    port: 9001
    targetPort: 9001
  type: ClusterIP
