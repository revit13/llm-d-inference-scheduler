apiVersion: v1
kind: Service
metadata:
  name: multimedia-downloader
  labels:
    app: multimedia-downloader
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: multimedia-downloader

