name: Test Sleep Workflow
on:
  workflow_dispatch:
  push:

jobs:
  sleep-test:
    runs-on: ubuntu-latest
    steps:
      - name: Sleep for 3 minutes
        run: |
          echo "Starting 3 minute sleep for testing..."
          echo "Current time: $(date)"
          sleep 180
          echo "Sleep completed at: $(date)"

# Made with Bob
